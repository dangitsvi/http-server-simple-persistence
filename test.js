var expect = require('chai').expect;
var chaiHttp = require('chai-http');
/*
describe('get route', function () {
  chaiHttp.request('http://localhost:8888')
      .get('/data')
      .end(function(err, res) {
        expect(err).to.be.null;
        expect(res).to.have.status(200);
      });

});*/
